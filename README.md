make sure python is installed

required packages:<br/>- PyQt6<br/>- requests<br/>- pyinstaller (for builds)

you gotta be on windows btw probably
then just run `build-release.bat` (or `build.bat` if you want the console)
ok bye